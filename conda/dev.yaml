name: irx
channels:
  - nodefaults
  - conda-forge
dependencies:
  - clang 14.*
  - clangdev 14.*
  - graphviz
  - pip
  - poetry >=2
  - python
  - nodejs # used by semantic-release
  - shellcheck
  - pip:
      # fix issues with distlib
      - paginate
      - pyrepl
